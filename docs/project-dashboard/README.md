# Project Dashboard Metrics

`snapshots.jsonl` stores one weekly dashboard snapshot per line. The
scheduled Project Dashboard workflow updates this file and publishes a
rendered dashboard to the repository wiki page:

https://github.com/gastownhall/gascity/wiki/Project-Dashboard

The raw data is intentionally checked in so public trend charts and status
changes can be reviewed like any other repository change.
