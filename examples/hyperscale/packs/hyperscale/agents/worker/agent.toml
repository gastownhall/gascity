scope = "rig"
start_command = "gascity-tools-fsm-test-agent --script {{.ConfigDir}}/assets/scripts/hyperscale-worker.yaml"
nudge = "Check your hook for work."
idle_timeout = "5m"
min_active_sessions = 0
max_active_sessions = 100
