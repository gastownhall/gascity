vulnerabilities:
  # Expiry horizon bulk-extended 2026-07-06: 2026-07-07 -> 2026-08-07.
  #
  # Rebuilt-from-source tools clear the Go-stdlib CVEs: contrib/k8s/Dockerfile.base
  # rebuilds gh and dolt, and contrib/k8s/Dockerfile.agent rebuilds bd, all with the
  # Go 1.26.5 toolchain, and each build asserts the artifact embeds patched grpc
  # (`go version -m ... google.golang.org/grpc`). Those three paths are therefore
  # NOT waived below: if a rebuilt bd/dolt/gh ever re-triggers a Go-stdlib CVE the
  # scan must fail so the regression is visible, not silently waived. Only the
  # still-external prebuilt binaries (br, kubectl) keep Go-stdlib waivers.
  #
  # The x/net, x/crypto, and thrift waivers further below are unaffected by the
  # rebuild: it bumped only google.golang.org/grpc, so bd/dolt/gc still pin the
  # vulnerable x/net/x/crypto module versions and keep those entries.
  #
  # Go stdlib CVEs disclosed 2026-05-12. Fixed in Go 1.25.10 / 1.26.3 for the
  # first batch (33811–42499); Go 1.26.4 / 1.25.11 for CVE-2026-42504 and
  # CVE-2026-27145; Go 1.26.5 / 1.25.12 for CVE-2026-39822. br and kubectl are
  # external prebuilt binaries still on Go 1.26.2 stdlib; remove each once its
  # upstream rebuilds against 1.26.5+. Durable full fix tracked in ga-frh27v.
  - id: CVE-2026-33811
    paths:
      - "usr/local/bin/br"
      - "usr/local/bin/kubectl"
    expired_at: 2026-09-21
    statement: External prebuilt br and kubectl still embed Go 1.26.2 stdlib; remove once each rebuilds against 1.26.3+ (or 1.25.10+). Rebuilt bd/dolt/gh (Go 1.26.5) clear this and are no longer waived.
  - id: CVE-2026-33814
    paths:
      - "usr/local/bin/br"
      - "usr/local/bin/kubectl"
    expired_at: 2026-09-21
    statement: External prebuilt br and kubectl still embed Go 1.26.2 stdlib; remove once each rebuilds against 1.26.3+ (or 1.25.10+). Rebuilt bd/dolt/gh (Go 1.26.5) clear this and are no longer waived. (gc's separate x/net http2 instance is waived below.)
  - id: CVE-2026-39820
    paths:
      - "usr/local/bin/br"
      - "usr/local/bin/kubectl"
    expired_at: 2026-09-21
    statement: External prebuilt br and kubectl still embed Go 1.26.2 stdlib; remove once each rebuilds against 1.26.3+ (or 1.25.10+). Rebuilt bd/dolt/gh (Go 1.26.5) clear this and are no longer waived.
  - id: CVE-2026-39822
    paths:
      - "usr/local/bin/br"
      - "usr/local/bin/kubectl"
    expired_at: 2026-09-21
    statement: Go stdlib os.Root symlink CVE (fixed Go 1.26.5 / 1.25.12); external prebuilt br and kubectl still affected pending upstream rebuilds. Rebuilt bd/dolt/gh (Go 1.26.5) clear it and are no longer waived; gc also builds with Go 1.26.5.
  - id: CVE-2026-39823
    paths:
      - "usr/local/bin/br"
      - "usr/local/bin/kubectl"
    expired_at: 2026-09-21
    statement: External prebuilt br and kubectl still embed Go 1.26.2 stdlib; remove once each rebuilds against 1.26.3+ (or 1.25.10+). Rebuilt bd/dolt/gh (Go 1.26.5) clear this and are no longer waived.
  - id: CVE-2026-39825
    paths:
      - "usr/local/bin/br"
      - "usr/local/bin/kubectl"
    expired_at: 2026-09-21
    statement: External prebuilt br and kubectl still embed Go 1.26.2 stdlib; remove once each rebuilds against 1.26.3+ (or 1.25.10+). Rebuilt bd/dolt/gh (Go 1.26.5) clear this and are no longer waived.
  - id: CVE-2026-39826
    paths:
      - "usr/local/bin/br"
      - "usr/local/bin/kubectl"
    expired_at: 2026-09-21
    statement: External prebuilt br and kubectl still embed Go 1.26.2 stdlib; remove once each rebuilds against 1.26.3+ (or 1.25.10+). Rebuilt bd/dolt/gh (Go 1.26.5) clear this and are no longer waived.
  - id: CVE-2026-39836
    paths:
      - "usr/local/bin/br"
      - "usr/local/bin/kubectl"
    expired_at: 2026-09-21
    statement: External prebuilt br and kubectl still embed Go 1.26.2 stdlib; remove once each rebuilds against 1.26.3+ (or 1.25.10+). Rebuilt bd/dolt/gh (Go 1.26.5) clear this and are no longer waived.
  - id: CVE-2026-42499
    paths:
      - "usr/local/bin/br"
      - "usr/local/bin/kubectl"
    expired_at: 2026-09-21
    statement: External prebuilt br and kubectl still embed Go 1.26.2 stdlib; remove once each rebuilds against 1.26.3+ (or 1.25.10+). Rebuilt bd/dolt/gh (Go 1.26.5) clear this and are no longer waived.
  - id: CVE-2026-42504
    paths:
      - "usr/local/bin/br"
      - "usr/local/bin/kubectl"
    expired_at: 2026-09-21
    statement: Go stdlib MIME-header DoS (CVE-2026-42504, fixed Go 1.26.4 / 1.25.11); external prebuilt br and kubectl still affected. Rebuilt bd/dolt/gh (Go 1.26.5) clear it and are no longer waived; gc cleared via go.mod toolchain.
  - id: CVE-2026-27145
    paths:
      - "usr/local/bin/kubectl"
    expired_at: 2026-09-21
    statement: Go stdlib x509 hostname verification issue (CVE-2026-27145), fixed in Go 1.26.4 / 1.25.11. kubectl in the base image still embeds Go 1.26.2; remove once the bundled CLI rebuilds against 1.26.4+. Rebuilt bd/dolt (Go 1.26.5) cleared and no longer waived.
  - id: CVE-2026-41602
    paths:
      - "usr/local/bin/dolt"
    expired_at: 2026-09-21
    statement: Latest Dolt 1.88.0 still embeds github.com/apache/thrift v0.13.1; remove after a Dolt release includes thrift 0.23.0 or later.
  - id: CVE-2026-56852
    paths:
      - "usr/bin/gh"
      - "usr/local/bin/dolt"
      - "usr/local/bin/kubectl"
    expired_at: 2026-09-21
    statement: Rebuilt gh and Dolt sources embed golang.org/x/text 0.38.0 and 0.36.0, and external kubectl embeds 0.33.0; remove each path when its upstream source bumps to x/text 0.39.0 or later.
  - id: CVE-2026-46600
    paths:
      - "usr/bin/gh"
      - "usr/local/bin/dolt"
      - "usr/local/bin/kubectl"
    expired_at: 2026-09-21
    statement: golang.org/x/net dns/dnsmessage DoS via invalid DNS record parsing; rebuilt gh and Dolt sources still pin x/net v0.53.0, fixed upstream in x/net 0.56.0, and external kubectl pins x/net v0.49.0. Short bridge only; durable fix for gh/dolt is the GH_SOURCE_REF/DOLT_SOURCE_REF rebuild tracked in ga-frh27v (see also contributor PR #5353); kubectl's fix is tracked in ga-rl1l10 — remove each path once its rebuild/bump lands. (docket E9, 2026-09-11 — added kubectl.)
  - id: CVE-2026-25680
    paths:
      - "usr/local/bin/dolt"
    expired_at: 2026-09-21
    statement: Dolt v2.1.7 still bundles a Go module set with this x/net HTML parsing issue; remove once upstream rebuilds against the fixed x/net release.
  - id: CVE-2026-25681
    paths:
      - "usr/local/bin/dolt"
    expired_at: 2026-09-21
    statement: Dolt v2.1.7 still bundles a Go module set with this x/net HTML rendering issue; remove once upstream rebuilds against the fixed x/net release.
  - id: CVE-2026-27136
    paths:
      - "usr/local/bin/dolt"
    expired_at: 2026-09-21
    statement: Dolt v2.1.7 still bundles a Go module set with this x/net HTML rendering issue; remove once upstream rebuilds against the fixed x/net release.
  - id: CVE-2026-39821
    paths:
      - "usr/local/bin/dolt"
    expired_at: 2026-09-21
    statement: Dolt v2.1.7 still bundles a Go module set with this x/net idna issue; remove once upstream rebuilds against the fixed x/net release.
  - id: CVE-2026-42502
    paths:
      - "usr/local/bin/dolt"
    expired_at: 2026-09-21
    statement: Dolt v2.1.7 still bundles a Go module set with this x/net HTML rendering issue; remove once upstream rebuilds against the fixed x/net release.
  - id: CVE-2026-42506
    paths:
      - "usr/local/bin/dolt"
    expired_at: 2026-09-21
    statement: Dolt v2.1.7 still bundles a Go module set with this x/net HTML rendering issue; remove once upstream rebuilds against the fixed x/net release.
  - id: CVE-2026-39827
    paths:
      - "usr/local/bin/dolt"
    expired_at: 2026-09-21
    statement: Dolt v2.1.7 still bundles golang.org/x/crypto v0.48.0; remove once upstream rebuilds against the fixed release.
  - id: CVE-2026-39828
    paths:
      - "usr/local/bin/dolt"
    expired_at: 2026-09-21
    statement: Dolt v2.1.7 still bundles golang.org/x/crypto v0.48.0; remove once upstream rebuilds against the fixed release.
  - id: CVE-2026-39829
    paths:
      - "usr/local/bin/dolt"
    expired_at: 2026-09-21
    statement: Dolt v2.1.7 still bundles golang.org/x/crypto v0.48.0; remove once upstream rebuilds against the fixed release.
  - id: CVE-2026-39830
    paths:
      - "usr/local/bin/dolt"
    expired_at: 2026-09-21
    statement: Dolt v2.1.7 still bundles golang.org/x/crypto v0.48.0; remove once upstream rebuilds against the fixed release.
  - id: CVE-2026-39831
    paths:
      - "usr/local/bin/dolt"
    expired_at: 2026-09-21
    statement: Dolt v2.1.7 still bundles golang.org/x/crypto v0.48.0; remove once upstream rebuilds against the fixed release.
  - id: CVE-2026-39832
    paths:
      - "usr/local/bin/dolt"
    expired_at: 2026-09-21
    statement: Dolt v2.1.7 still bundles golang.org/x/crypto v0.48.0; remove once upstream rebuilds against the fixed release.
  - id: CVE-2026-39835
    paths:
      - "usr/local/bin/dolt"
    expired_at: 2026-09-21
    statement: Dolt v2.1.7 still bundles golang.org/x/crypto v0.48.0; remove once upstream rebuilds against the fixed release.
  - id: CVE-2026-42508
    paths:
      - "usr/local/bin/dolt"
    expired_at: 2026-09-21
    statement: Dolt v2.1.7 still bundles golang.org/x/crypto v0.48.0; remove once upstream rebuilds against the fixed release.
  - id: CVE-2026-46595
    paths:
      - "usr/local/bin/dolt"
    expired_at: 2026-09-21
    statement: Dolt v2.1.7 still bundles golang.org/x/crypto v0.48.0; remove once upstream rebuilds against the fixed release.
  - id: CVE-2026-46597
    paths:
      - "usr/local/bin/dolt"
    expired_at: 2026-09-21
    statement: Dolt v2.1.7 still bundles golang.org/x/crypto v0.48.0; remove once upstream rebuilds against the fixed release.
  # The golang.org/x/net (HTML/idna/http2) and golang.org/x/crypto/ssh CVEs in
  # the same series the dolt entries above waive are also reported against the
  # bd CLI binary (steveyegge/beads v1.1.0, external), the gc binary, and the
  # external kubectl binary (golang.org/x/net v0.49.0; the gc-controller image
  # adds kubectl on top of the agent image). With set -e the Container Scan
  # halts at the first failing image, so kubectl's x/net findings stayed masked
  # until the bd/gc findings were waived.
  #
  # This change bumps gc's go.mod to golang.org/x/net v0.54.0 and
  # golang.org/x/crypto v0.52.0, which clears the gc instance of every
  # x/crypto/ssh CVE below (fixed in x/crypto 0.52.0) and CVE-2026-33814
  # (x/net http2, fixed in 0.53.0); the gc path is therefore dropped from those
  # entries so the scan must prove gc is clean rather than mask it. bd (beads v1.1.0) and
  # kubectl stay external and base-pre-existing (Container Scan already red on
  # main, scheduled run 2026-06-24); remove their paths once they rebuild
  # upstream. TestTrivyIgnoreDropsGCModuleWaiversPastThreshold enforces that no
  # gc waiver outlives its go.mod fix version.
  - id: CVE-2026-25680
    paths:
      - "usr/local/bin/bd"
      - "usr/local/bin/kubectl"
    expired_at: 2026-09-21
    statement: golang.org/x/net HTML parsing DoS; base-pre-existing (also red on main 2026-06-24). Present in bd (beads v1.1.0) and kubectl (external, x/net v0.49.0). Remove once bd/kubectl rebuild upstream.
  - id: CVE-2026-25681
    paths:
      - "usr/local/bin/bd"
      - "usr/local/bin/kubectl"
    expired_at: 2026-09-21
    statement: golang.org/x/net HTML rendering issue; base-pre-existing (also red on main 2026-06-24). Present in bd (beads v1.1.0) and kubectl (external, x/net v0.49.0). Remove once bd/kubectl rebuild upstream.
  - id: CVE-2026-27136
    paths:
      - "usr/local/bin/bd"
      - "usr/local/bin/kubectl"
    expired_at: 2026-09-21
    statement: golang.org/x/net HTML rendering issue; base-pre-existing (also red on main 2026-06-24). Present in bd (beads v1.1.0) and kubectl (external, x/net v0.49.0). Remove once bd/kubectl rebuild upstream.
  - id: CVE-2026-39821
    paths:
      - "usr/local/bin/bd"
      - "usr/local/bin/kubectl"
    expired_at: 2026-09-21
    statement: golang.org/x/net/idna issue; base-pre-existing (also red on main 2026-06-24). Present in bd (beads v1.1.0) and kubectl (external, x/net v0.49.0). Remove once bd/kubectl rebuild upstream.
  - id: CVE-2026-42502
    paths:
      - "usr/local/bin/bd"
      - "usr/local/bin/kubectl"
    expired_at: 2026-09-21
    statement: golang.org/x/net HTML rendering issue; base-pre-existing (also red on main 2026-06-24). Present in bd (beads v1.1.0) and kubectl (external, x/net v0.49.0). Remove once bd/kubectl rebuild upstream.
  - id: CVE-2026-42506
    paths:
      - "usr/local/bin/bd"
      - "usr/local/bin/kubectl"
    expired_at: 2026-09-21
    statement: golang.org/x/net HTML rendering issue; base-pre-existing (also red on main 2026-06-24). Present in bd (beads v1.1.0) and kubectl (external, x/net v0.49.0). Remove once bd/kubectl rebuild upstream.
  - id: CVE-2026-39827
    paths:
      - "usr/local/bin/bd"
    expired_at: 2026-09-21
    statement: golang.org/x/crypto/ssh CVE; present in bd (beads v1.1.0). gc cleared by the golang.org/x/crypto v0.52.0 bump in this change. Remove once bd rebuilds against x/crypto >= 0.52.0.
  - id: CVE-2026-39828
    paths:
      - "usr/local/bin/bd"
    expired_at: 2026-09-21
    statement: golang.org/x/crypto/ssh CVE; present in bd (beads v1.1.0). gc cleared by the golang.org/x/crypto v0.52.0 bump in this change. Remove once bd rebuilds against x/crypto >= 0.52.0.
  - id: CVE-2026-39829
    paths:
      - "usr/local/bin/bd"
    expired_at: 2026-09-21
    statement: golang.org/x/crypto/ssh CVE; present in bd (beads v1.1.0). gc cleared by the golang.org/x/crypto v0.52.0 bump in this change. Remove once bd rebuilds against x/crypto >= 0.52.0.
  - id: CVE-2026-39830
    paths:
      - "usr/local/bin/bd"
    expired_at: 2026-09-21
    statement: golang.org/x/crypto/ssh CVE; present in bd (beads v1.1.0). gc cleared by the golang.org/x/crypto v0.52.0 bump in this change. Remove once bd rebuilds against x/crypto >= 0.52.0.
  - id: CVE-2026-39831
    paths:
      - "usr/local/bin/bd"
    expired_at: 2026-09-21
    statement: golang.org/x/crypto/ssh CVE disclosed 2026-07; present in bd (beads v1.1.0). gc cleared by the golang.org/x/crypto v0.52.0 bump in this change. Remove once bd rebuilds against x/crypto >= 0.52.0.
  - id: CVE-2026-39832
    paths:
      - "usr/local/bin/bd"
    expired_at: 2026-09-21
    statement: golang.org/x/crypto/ssh/agent CVE; present in bd (beads v1.1.0). gc cleared by the golang.org/x/crypto v0.52.0 bump in this change. Remove once bd rebuilds against x/crypto >= 0.52.0.
  - id: CVE-2026-39835
    paths:
      - "usr/local/bin/bd"
    expired_at: 2026-09-21
    statement: golang.org/x/crypto/ssh CVE; present in bd (beads v1.1.0). gc cleared by the golang.org/x/crypto v0.52.0 bump in this change. Remove once bd rebuilds against x/crypto >= 0.52.0.
  - id: CVE-2026-42508
    paths:
      - "usr/local/bin/bd"
    expired_at: 2026-09-21
    statement: golang.org/x/crypto/ssh/knownhosts CVE; present in bd (beads v1.1.0). gc cleared by the golang.org/x/crypto v0.52.0 bump in this change. Remove once bd rebuilds against x/crypto >= 0.52.0.
  - id: CVE-2026-46595
    paths:
      - "usr/local/bin/bd"
    expired_at: 2026-09-21
    statement: golang.org/x/crypto/ssh CVE; present in bd (beads v1.1.0). gc cleared by the golang.org/x/crypto v0.52.0 bump in this change. Remove once bd rebuilds against x/crypto >= 0.52.0.
  - id: CVE-2026-46597
    paths:
      - "usr/local/bin/bd"
    expired_at: 2026-09-21
    statement: golang.org/x/crypto/ssh CVE; present in bd (beads v1.1.0). gc cleared by the golang.org/x/crypto v0.52.0 bump in this change. Remove once bd rebuilds against x/crypto >= 0.52.0.
  - id: CVE-2026-56854
    paths:
      - "usr/bin/gh"
      - "usr/local/bin/dolt"
      - "usr/local/bin/bd"
    expired_at: 2026-09-21
    statement: golang.org/x/crypto/ssh CVE (CRITICAL); carried by gh (x/crypto v0.53.0), Dolt (v0.50.0), and bd (v0.53.0) only — kubectl does not bundle these binaries. Short bridge only; durable fix is the gh/Dolt module rebuild tracked in contributor pull request 5353 (active again since 2026-09-08), plus rebuilding bd from beads main, whose go.mod already pins golang.org/x/crypto >= v0.54.0 — remove each path once its rebuild lands.
  # Operator ruling docket E9 (2026-09-11, "widen #5885's .trivyignore.yaml to
  # cover every HIGH/CRITICAL finding failing its scan today"): PR #5885's own
  # Container Scan run (34553600725 / job 103121488224, "Image vulnerabilities")
  # still failed on the findings below after the docket D6 widening above. Each
  # entry is scoped to only the images/paths or purls actually reported on that
  # run; nothing is waived beyond that explicit list. Go-stdlib CVEs are
  # deliberately NOT waived for gh/dolt/bd (see the file header) since those
  # three clear stdlib CVEs via the Go 1.26.5 rebuild.
  - id: CVE-2026-56864
    paths:
      - "usr/bin/gh"
    expired_at: 2026-09-21
    statement: golang.org/x/mod CVE; rebuilt gh source still pins x/mod v0.37.0, fixed upstream in x/mod v0.40.0. Durable fix is the GH_SOURCE_REF rebuild tracked in contributor pull request 5353 — remove once that source bump lands.
  - id: CVE-2026-56865
    paths:
      - "usr/bin/gh"
    expired_at: 2026-09-21
    statement: golang.org/x/mod CVE; rebuilt gh source still pins x/mod v0.37.0, fixed upstream in x/mod v0.40.0. Durable fix is the GH_SOURCE_REF rebuild tracked in contributor pull request 5353 — remove once that source bump lands.
  - id: CVE-2026-84304
    paths:
      - "usr/bin/gh"
      - "usr/local/bin/dolt"
      - "usr/local/bin/bd"
      - "usr/local/bin/gc"
    expired_at: 2026-09-21
    statement: google.golang.org/grpc CVE; rebuilt gh and Dolt sources pin grpc v1.82.1, durable fix is the GH_SOURCE_REF/DOLT_SOURCE_REF rebuild tracked in contributor pull request 5353 (fixed upstream in grpc v1.83.1); bd and gc pin grpc v1.83.0, fixed in v1.83.1, tracked in ga-rl1l10. Remove each path once its rebuild/bump lands.
  - id: CVE-2026-84445
    paths:
      - "usr/bin/gh"
      - "usr/local/bin/dolt"
      - "usr/local/bin/bd"
      - "usr/local/bin/gc"
    expired_at: 2026-09-21
    statement: google.golang.org/grpc CVE; rebuilt gh and Dolt sources pin grpc v1.82.1, durable fix is the GH_SOURCE_REF/DOLT_SOURCE_REF rebuild tracked in contributor pull request 5353 (fixed upstream in grpc v1.83.1); bd and gc pin grpc v1.83.0, fixed in v1.83.1, tracked in ga-rl1l10. Remove each path once its rebuild/bump lands.
  - id: CVE-2026-43871
    paths:
      - "usr/local/bin/dolt"
      - "usr/local/bin/bd"
    expired_at: 2026-09-21
    statement: github.com/apache/thrift CVE; rebuilt Dolt source still pins thrift v0.13.1-0.20201008052519-daf620915714, durable fix is the DOLT_SOURCE_REF rebuild tracked in contributor pull request 5353; bd pins thrift v0.23.0, fixed in v0.24.0, tracked in ga-rl1l10. Remove each path once its rebuild/bump lands.
  - id: CVE-2026-33818
    paths:
      - "usr/local/bin/kubectl"
    expired_at: 2026-09-21
    statement: Go stdlib CVE; external prebuilt kubectl still embeds Go 1.26.2 stdlib, fixed in Go 1.26.6. Remove once kubectl rebuilds against 1.26.6+. Tracked in ga-rl1l10.
  - id: CVE-2026-56853
    paths:
      - "usr/local/bin/kubectl"
    expired_at: 2026-09-21
    statement: Go stdlib CVE; external prebuilt kubectl still embeds Go 1.26.2 stdlib, fixed in Go 1.26.6. Remove once kubectl rebuilds against 1.26.6+. Tracked in ga-rl1l10.
  - id: CVE-2026-56858
    paths:
      - "usr/local/bin/kubectl"
    expired_at: 2026-09-21
    statement: Go stdlib CVE; external prebuilt kubectl still embeds Go 1.26.2 stdlib, fixed in Go 1.26.6. Remove once kubectl rebuilds against 1.26.6+. Tracked in ga-rl1l10.
  - id: CVE-2026-56859
    paths:
      - "usr/local/bin/kubectl"
    expired_at: 2026-09-21
    statement: Go stdlib CVE; external prebuilt kubectl still embeds Go 1.26.2 stdlib, fixed in Go 1.26.6. Remove once kubectl rebuilds against 1.26.6+. Tracked in ga-rl1l10.
  - id: CVE-2026-56860
    paths:
      - "usr/local/bin/kubectl"
    expired_at: 2026-09-21
    statement: Go stdlib CVE; external prebuilt kubectl still embeds Go 1.26.2 stdlib, fixed in Go 1.26.6. Remove once kubectl rebuilds against 1.26.6+. Tracked in ga-rl1l10.
  - id: CVE-2026-56862
    paths:
      - "usr/local/bin/kubectl"
    expired_at: 2026-09-21
    statement: Go stdlib CVE; external prebuilt kubectl still embeds Go 1.26.2 stdlib, fixed in Go 1.26.6. Remove once kubectl rebuilds against 1.26.6+. Tracked in ga-rl1l10.
  - id: CVE-2026-53612
    purls:
      - "pkg:deb/debian/bsdutils"
      - "pkg:deb/debian/libblkid1"
      - "pkg:deb/debian/liblastlog2-2"
      - "pkg:deb/debian/libmount1"
      - "pkg:deb/debian/libsmartcols1"
      - "pkg:deb/debian/libuuid1"
      - "pkg:deb/debian/login"
      - "pkg:deb/debian/mount"
      - "pkg:deb/debian/util-linux"
    expired_at: 2026-09-21
    statement: Debian 13.5 util-linux source package CVE across the gc-mcp-mail image's bsdutils/libblkid1/liblastlog2-2/libmount1/libsmartcols1/libuuid1/login/mount/util-linux binary packages, pinned at 2.41-5, fixed in 2.41.5-0+deb13u1. Tracked in ga-dkgeoi; remove once the base image rebuilds against the patched Debian package. These purls appear only in gc-mcp-mail's own package set, which confines the waiver without a paths entry.
  - id: CVE-2026-53613
    purls:
      - "pkg:deb/debian/bsdutils"
      - "pkg:deb/debian/libblkid1"
      - "pkg:deb/debian/liblastlog2-2"
      - "pkg:deb/debian/libmount1"
      - "pkg:deb/debian/libsmartcols1"
      - "pkg:deb/debian/libuuid1"
      - "pkg:deb/debian/login"
      - "pkg:deb/debian/mount"
      - "pkg:deb/debian/util-linux"
    expired_at: 2026-09-21
    statement: Debian 13.5 util-linux source package CVE across the gc-mcp-mail image's bsdutils/libblkid1/liblastlog2-2/libmount1/libsmartcols1/libuuid1/login/mount/util-linux binary packages, pinned at 2.41-5, fixed in 2.41.5-0+deb13u1. Tracked in ga-dkgeoi; remove once the base image rebuilds against the patched Debian package. These purls appear only in gc-mcp-mail's own package set, which confines the waiver without a paths entry.
  - id: CVE-2026-53614
    purls:
      - "pkg:deb/debian/bsdutils"
      - "pkg:deb/debian/libblkid1"
      - "pkg:deb/debian/liblastlog2-2"
      - "pkg:deb/debian/libmount1"
      - "pkg:deb/debian/libsmartcols1"
      - "pkg:deb/debian/libuuid1"
      - "pkg:deb/debian/login"
      - "pkg:deb/debian/mount"
      - "pkg:deb/debian/util-linux"
    expired_at: 2026-09-21
    statement: Debian 13.5 util-linux source package CVE across the gc-mcp-mail image's bsdutils/libblkid1/liblastlog2-2/libmount1/libsmartcols1/libuuid1/login/mount/util-linux binary packages, pinned at 2.41-5, fixed in 2.41.5-0+deb13u1. Tracked in ga-dkgeoi; remove once the base image rebuilds against the patched Debian package. These purls appear only in gc-mcp-mail's own package set, which confines the waiver without a paths entry.
  - id: CVE-2026-78676
    purls:
      - "pkg:pypi/gitpython"
    expired_at: 2026-09-21
    statement: GitPython CRITICAL remote-code-execution CVE; gc-mcp-mail pins GitPython 3.1.58, fixed in 3.1.59. Tracked in ga-dkgeoi; remove once the image bumps the pinned dependency. This purl appears only in gc-mcp-mail's own package set, which confines the waiver without a paths entry.
  - id: CVE-2026-78675
    purls:
      - "pkg:pypi/gitpython"
    expired_at: 2026-09-21
    statement: GitPython CVE; gc-mcp-mail pins GitPython 3.1.58, fixed in 3.1.59. Tracked in ga-dkgeoi; remove once the image bumps the pinned dependency. This purl appears only in gc-mcp-mail's own package set, which confines the waiver without a paths entry.
  - id: CVE-2026-78677
    purls:
      - "pkg:pypi/gitpython"
    expired_at: 2026-09-21
    statement: GitPython CVE; gc-mcp-mail pins GitPython 3.1.58, fixed in 3.1.59. Tracked in ga-dkgeoi; remove once the image bumps the pinned dependency. This purl appears only in gc-mcp-mail's own package set, which confines the waiver without a paths entry.
